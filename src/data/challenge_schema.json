{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CodingChallenge",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "The prompt or task shown to the user in the terminal."
    },
    "validate": {
      "type": "string",
      "description": "A Python expression that must evaluate to True for the challenge to succeed."
    },
    "hint": {
        "type": "string",
        "description": "Optional guidance for the user."
    }
  },
  "required": ["description", "validate"]
}
